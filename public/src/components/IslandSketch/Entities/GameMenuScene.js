// GameMenuScene.js
import { GameScene } from './GameScene';

export class GameMenuScene extends GameScene {
  constructor() {
    super('GameMenuScene');
  }

  preload(p5) {
    // Load menu assets
  }

  setup(p5, canvasParentRef) {
    // Setup menu
  }

  draw(p5) {
    // Draw menu
  }
}
