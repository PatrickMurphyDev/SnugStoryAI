// GameDialogueScene.js
import { GameScene } from '../Scenes/GameScene';

export class GameSlideScene extends GameScene {
  constructor(typeName) {
    super(typeName || 'GameSlideScene');
  }

  preload(p5) {
    // Load dialogue assets
  }

  setup(p5, canvasParentRef) {
    // Setup dialogue
  }

  draw(p5) {
    // Draw dialogue
  }
}