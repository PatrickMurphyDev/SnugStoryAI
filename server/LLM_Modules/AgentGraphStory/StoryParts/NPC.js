export class NPC {
    /**
     * @param {Character[]} characters - Comprehensive list of characters with their roles and backstories
     */
    constructor(characters = []) {
        this.characters = characters;
    }
}
